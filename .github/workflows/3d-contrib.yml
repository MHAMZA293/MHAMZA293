name: Generate 3D Contribution Graph

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "The old 3D contribution graph action is no longer available."
